{
    "private": true,
    "type": "module",
    "scripts": {
        "dev:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-tree-view.css --postcss --watch",
        "dev:scripts": "node bin/build.js --dev",
        "build:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-tree-view.css --postcss --minify",
        "build:scripts": "node bin/build.js",
        "purge": "filament-purge -i resources/dist/filament-tree-view.css -o resources/dist/filament-tree-view.css",
        "dev": "npm-run-all --parallel dev:*",
        "build": "npm-run-all build:*"
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.0.6",
        "esbuild": "^0.25.0",
        "npm-run-all": "^4.1.5"
    },
    "dependencies": {
        "sortablejs": "^1.15.6"
    }
}
